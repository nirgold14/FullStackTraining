First Project:


    go to your project dir
    run: npm init.
    it will create: package.json.
    
    install the package: express -> which handles all HTTP requests
    at package.json, it will add to "dependecies" which mean this project need those.
    
    by using the function listen, we open a new socket with the required port.
    now we can see it at: localhost:3000.
    
    the get function activate when / basic
    